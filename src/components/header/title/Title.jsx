import React from 'react';
import style from './Title.module.scss'

const Title = () => {
  return(
    <div className={style.wrapper}>
      <h2 className={style.title}>Social network</h2>
    </div>
  )
}

export default Title;