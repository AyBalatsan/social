import React from 'react';
import style from './Settings.module.scss';

const Settings = () => {
  return(
    <main className={style.wrapper}>
      <h1>Hello, im Settings</h1>
    </main>
  )
}

export default Settings;